import setuptools

setuptools.setup(
    name = 'radforest',
    version = '0.1.0',
    description = 'This is the rad forest generator',
    url ='',
    author ='danEscobar',
    author_email='',
    licence='MIT',
    packages=setuptools.find_packages(exclude=['tests'])
)